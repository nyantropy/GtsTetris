#pragma once

namespace TetrisConstants
{
    inline constexpr float SOFTDROP_INTERVAL = 0.05f;
    inline constexpr float MOVE_INTERVAL     = 0.08f;
    inline constexpr float ROTATE_INTERVAL   = 0.15f;
    inline constexpr float LOCK_DELAY_LIMIT  = 0.5f;
}
